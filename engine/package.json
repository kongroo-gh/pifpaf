{
  "name": "@pifpaf/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pif Paf のルールエンジン（UI・通信に非依存の純粋ロジック）。将来はNode.jsサーバーからもそのまま利用する想定。",
  "main": "index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
