# CLAUDE.md — Pif Paf Web化プロジェクト

このファイルはClaude Codeがこのリポジトリで作業する際の前提コンテキストです。
チャット版Claudeとの事前セッションで決めた設計方針と現状を引き継いでいます。

## プロジェクトの目的
ブラジルで人気のトランプゲーム「Pif Paf（Pife / Cacheta とも呼ばれるラミー系ゲーム）」の
Webゲーム化。まずは人間1人 + CPU対戦から作り、将来的にオンラインのリアルタイム対戦へ拡張する。
ポイント制（Cacheta方式のマッチ）は実装済み。掛け金として見せている。

## 確定済みの仕様判断（ユーザー確認済み）
- **ヴィラ（vira）ワイルドルール：採用する**。配札後に山札から1枚公開し、
  **その次のランクかつヴィラと同じスートの1種類だけ**がワイルドになる
  （例：ヴィラが 7♠ → 8♠ だけがワイルド。8♥8♦8♣ は普通の札）。
  2組デッキなのでワイルドは103枚中わずか2枚。詳細は `rules.md` 参照。
  - ワイルドは `Wild { rank, suit }` 型で持ち回る。`Rank` 単体で渡していた頃の名残で
    `wildRank` を探すと見つからないので注意。判定は `isWildCard(card, wild)` に集約。
  - **ランクだけの一致でワイルド扱いにしないこと。** 緩めるとワイルドが8枚に戻り
    ゲームが別物になる。`melds.test.ts` の「ワイルドの範囲」がこれを守っている。
- **プレイ人数：4人固定**
- **対戦形式：将来オンライン化前提で、まずはCPU対戦から実装する**
  → これが最重要のアーキテクチャ制約。詳細は次項。

## アーキテクチャ方針（なぜこの構成か）
カードゲームは「手札を隠す」という性質上、オンライン化する際はサーバー側で
状態を権威的に管理し、各クライアントには自分の手札しか見せない設計が必須になる。
これを後から追加すると大きな手戻りになるため、**最初からロジックをUI・通信から
完全に分離**しておく。

```
pifpaf/                  npm workspaces のルート
├── rules.md             ルール仕様書（仮定・要調整点も明記）
├── CLAUDE.md             このファイル
├── engine/               純粋なゲームロジック（React/DOM/通信に一切依存しない）
│   ├── index.ts          公開窓口（バレル）。web/ はここ経由でのみ参照する
│   ├── types.ts          Card / Suit / Rank
│   ├── deck.ts           デッキ生成・シャッフル・配札・ヴィラ決定
│   ├── melds.ts          トリンカ／シーケンス判定、手札の役分類（ワイルド対応）
│   ├── gameEngine.ts     ターンの状態遷移（reducer）。DRAW / DISCARD / BATER
│   ├── ai.ts             CPU用の簡易AI（貪欲法）
│   ├── match.ts          マッチ（チップ・失点・脱落・配当）
│   ├── *.test.ts         Vitestテスト（100件・全て通過）
│   ├── sanity-check.ts   npm installなしで `node --experimental-strip-types` で動作確認できるスクリプト
│   └── package.json / tsconfig.json
├── protocol/            オンライン対戦の通信仕様。web と server の両方が参照する
│   ├── view.ts          席ごとに削った盤面の型（PublicGameState / PlayerView）
│   ├── mask.ts          削る純粋関数。**ここが破れると対戦が成立しない**
│   └── messages.ts      往復するメッセージと、受信時の検証
├── server/              オンライン対戦の権威サーバー
│   ├── room.ts          1卓の状態と規則。**通信手段もタイマーも知らない**
│   ├── ws.ts            WebSocket（標準ライブラリのみ。ws は使わない）
│   ├── hub.ts           卓と接続の橋渡し。CPUの「間合い」もここ
│   └── index.ts         起動口
└── web/                  React + Vite のUI。engineを呼び出すだけで、
    ├── src/game/         ゲームルール自体は一切持たない
    ├── src/net/          オンライン対戦（接続層と卓の画面）
    │   ├── useGame.ts    状態保持とCPUのタイマー駆動（副作用はここに閉じ込める）
    │   ├── useExecution.ts 決着後の演出タイミング
    │   └── players.ts    席の設定（演出のみ。engineは席番号しか知らない）
    ├── src/components/   カード・座席・卓上の判断
    └── src/App.tsx / styles.css
```

## 画面サイズ対応（踏んだ落とし穴）
- **幅だけのメディアクエリでは横向きが漏れる。** `@media (max-width: 700px)` だけだと
  844x390 のような横向きが素通りしてデスクトップ配置のままになり、高さが300px以上
  溢れてボタンが画面外に出た。`(max-width: 700px), (max-height: 560px)` の
  **OR** で「狭い」か「低い」かの両方を拾うこと。
- 縦向きは相手3人を**3列の要約**（肩書きと扇状の裏面を落として名前＋枚数だけ）に
  組み替える。縦積みのままだと相手だけで453px＝画面の56%を食う。
- 横向きは幅が余るので手札を**1行**（`flex-wrap: nowrap`）にして縦を稼ぐ。
- iOS Safari はツールバーの伸縮で `100vh` がずれるため `100dvh` を使う。
  ノッチとホームインジケータのぶんは `env(safe-area-inset-*)` で確保
  （`index.html` の `viewport-fit=cover` とセット）。
- 小さいカードでは右下の角表示を消す。左上の繰り返しなうえ、CORINGA の表示と重なる。
- タッチ端末では hover の持ち上げが誤作動するので `@media (hover: none)` で止め、
  選択時だけ動かす。

確認済み: 375x667 / 360x780 / 390x844（縦）、667x375 / 844x390（横）で
スクロールなし・横溢れなし・ボタン可視。

## ポイント制（マッチ）
本家 **Cacheta** の方式。点を稼ぐのではなく命（vidas）を減らし、0で脱落・
最後の1人が勝ち。この実装では命をチップ＝掛け金として見せている。
勘定は `engine/match.ts` が全部持つ（純粋関数）。詳細は `rules.md` 4.1節。

- 7チップ開始 / 負け2・降り1・10枚上がりを食らうと3 / 実測 約6ラウンド4〜5分
- 配当 =（残りチップ 2.7〜4.5倍 ＋ 連勝ボーナス 最大+1.2）× ワイルド係数(0.75 or 1.0)
- 20万回の模擬で寺銭24%に合わせてある。**数字を触るなら必ず模擬し直すこと**

**この変更で演出の意味が変わった。** 以前は毎ラウンドが決着だったが、
いまは**チップが尽きて脱落した瞬間だけ**が節目になる。金が降るのもマッチ制覇時だけ。

踏んだこと:
- `settleRound` は `round` を進めた状態を返すので、結果表示中の
  ラウンド番号は1つ戻して見せる必要がある
- 脱落者は「降りた者」と同じく `folded` で手番を飛ばす。
  席番号を保ったまま人数を減らせるので、engine側に人数変更の概念が要らない

## トリンカの枚数別ルール（間違えやすい）
**重複すべき記号の数 = 枚数 − 3。** 3枚組だけ「全て異なる」で、4枚以上は
逆に重複が要る。7♠7♥7♦7♣ のように4種類すべて違う形は4枚組として認めない。

| 枚数 | 重複 |
|---|---|
| 3枚 | 0（全て異なる） |
| 4枚 | 1つ |
| 5枚 | 2つ |
| 6枚 | 組として存在しない（3枚組が2つになる） |

ワイルドは記号の割り当てを総当たりして判定している（`suitsSatisfyTrinca`）。
高々2枚しか無いので探索は小さい。
`melds.test.ts` の「トリンカの枚数別ルール」がこれを固定している。

実測（200局）の上がりの形: 3+3+3 が159、3+3+4 が35、4+5 が6。
5枚組は稀だが経路として機能しており、割り込み率・ラウンド長への影響は無かった。

階段の A を両端で使えるようにした後（800局）: 10枚上がり 20.4%（95%区間 17.6〜23.2）、
割り込み決着 82.4%、1局 42.4手。以前の 17.5% は200局の標本なので、
差が本物かは区間が重なっていて判別できていない。**配当表は再計算していない**
（20万回の模擬が要る）。10枚上がりは失点が2ではなく3なので、
本当に増えているならマッチがわずかに短くなる方向に効く。

## 表示言語（i18n）
`web/src/i18n/` に閉じている。**engine には持ち込まない**（ルールは言語に依存しないし、
engine を UI から独立させる原則も崩れる）。engine が返すエラー文言は日本語のままだが、
これは開発用で画面には出していない。

- `types.ts` の `Strings` が**翻訳の契約**。言語を足す作業は「この型を満たす辞書を
  1本書いて `index.tsx` の `DICTS` に載せる」だけ。**埋め忘れは `tsc --noEmit` が全部並べる**
- `ja.ts` / `en.ts` / `pt.ts` の3言語
- 参照は `useT()`。席の呼び名だけは `personaName(t, seat)`
  （`noUncheckedIndexedAccess` があるので添字を直接引かない）

決めごと:
- **ポルトガル語の装飾語は訳さない。** `BATER!` `SENTAR À MESA` `CORINGA` `FALIDO` などは
  卓の雰囲気そのもの。訳すのはその下の小さな注釈だけ。だから辞書に入っているのは
  「注釈」であって「見出し」ではない
- **カードの表記も訳さない。** `7♠` `A-2-3` は言語に依存しないので JSX に直接書く
- **数を含む文は関数にする**（`round: (n) => ...`）。英語の単複や語順は差し込みでは吸収できない
- **強調は辞書側で `*` を挟み、`<Rich>` が開く。** 訳文ごとに強調位置が動くため、
  JSX に `<strong>` を直接書くと訳の数だけ構造を作り直す羽目になる
- 選択は localStorage の `pifpaf.lang`。無ければブラウザの言語（`pt-BR` は `pt` に丸める）、
  それも駄目なら日本語

**ポルトガル語で装飾語と訳が重なる問題。** 装飾語はもともとポルトガル語なので、
その言語で読むと「AJUSTES — Ajustes」「FIM DA RODADA — fim da rodada」
「O Chefe — O Chefe」のように同じ語が2回出る。辞書側を空にして回ることもできるが、
言語を足すたびに同じ罠を踏むので、**突き合わせて自動で落とす**ようにした。

- `withGloss(装飾語, 訳)` … 大小文字とアクセントを無視して一致すれば訳を捨てる
- `<Kicker flavor gloss className>` … 見出し用。`<Gloss flavor text>` … ボタンの `<small>` 用
- 新しく「ポルトガル語の見出し＋訳」を足すときは、直に `—` で繋がずこれらを使うこと

## 設定（言語・CPUの速さ）
`Settings.tsx`。同じ `SettingsControls` を2通りに置いている。

- **卓に着く前**（イントロ・掛け金画面・オンラインの入口）は選択肢をそのまま並べる。
  最初に決めてもらいたいので、開く操作を挟まない。掛け金画面では
  「一文無し」の枝でも出るよう条件の外に置いてある
- **対局中**はヘッダー右肩の歯車から開く（`SettingsPanel`）。盤面に常時出す余地がない。
  **オンライン卓も同じ**（以前ここには歯車が無く、言語を変える手段が無かった）
- **速さは任意の props**。`speed` / `onSpeed` を渡さなければ言語だけになる。
  オンラインではCPUをサーバーが動かしているので、速さを出すと効かない設定を
  見せることになる。置き場所と開き方だけ単機版と揃える
- ルールブックの見出しにも言語だけ置いた（`LanguagePills`）。
  読んでいる最中が、言語を変えたくなる一番ありそうな瞬間なので
- ヘッダーの `?` と歯車は文字を持たない `.iconButton`。
  以前は速さと言語のトグルを文字つきで並べていたが、言語が3つに増えると入らない

## 効果音（2026-09-03）
`web/src/audio/`。**音源ファイルは1つも持たず、すべて Web Audio で合成する。**
卓の音は短い破裂音と減衰音ばかりで合成で足りるうえ、資産を持つと GitHub Pages に
数百KBと出典・利用条件の管理が増える。ビルドは +9KB（gzip +3KB）で済んでいる。

```
audio/
├── context.ts    AudioContext・音量・消音の保存・解禁。合成の道具（雑音と包絡）もここ
├── sfx.ts        一発もの（札・チップ・金貨・上がり・破産・番の合図…）
├── ambience.ts   イントロで流す環境音の仕掛け。**ここだけがループする**
├── tunes/        その曲。何を鳴らすかだけを持ち、仕掛けは知らない
│   ├── types.ts  Tune の形（刻み・揺らぎ・層・目盛りごとに鳴るもの）
│   ├── noir.ts   不穏なドローン（2026-09-03）。**差し戻し先**
│   └── bossa.ts  ボサノヴァ（2026-09-04・ユーザー指示で現在こちら）
└── index.ts      画面から触る窓口。useSoundUnlock / useAmbience / useSoundControl
```

- 環境音には段取りがある（2026-09-04・ユーザー指示）。
  **静けさ → 音量が上がりながら曲が始まる → しばらく流れる → 銃声が一発 →
  断ち切られて無音 → 少し置いて、また音量が上がる**。最初もここから始まる。
  隣の部屋のラジオが鳴っていて、外で一発鳴るたびに止まる、という見立て
  - 撃たれるまでの長さは 17〜26 秒の間で毎回変える。同じ秒数だと身構えられる
  - **銃声は曲とは別の口（`shots`）に流す。** 曲を断つ操作が銃声まで巻き込むと、
    撃った音そのものが消える。逆に、卓に着いたときは予約済みの銃声も黙らせる
  - この銃声は「撃たれる演出」ではない（下の「見た目の方針」を参照）
- 環境音の曲は `ambience.ts` の `TUNE` が選ぶ。**差し替えも差し戻しも1行**。
  どちらもニ短調で、違うのは脈の打ち方だけ（不穏さの芯だった増四度は、
  ボサノヴァでは A7♭9 の3度と7度として同じ濁りのまま残してある）
- **どちらの曲にも旋律を置かない。** 覚えられる節があると、2回目から
  待ち時間の長さが気になり始める

- **画面に「鳴らせるか」を判断させない。** 消音中でも操作前でも `sfx.card()` は
  呼んでよく、鳴らないだけ。条件を呼ぶ側に持たせると同じ判定が画面の数だけ増える
- **ブラウザは操作前に音を出させない。** `useSoundUnlock()` を `App` で一度だけ呼び、
  最初の pointerdown / keydown で `resume()` する。それまでに求められた音は捨てる
  （溜めて後から一斉に鳴らすほうが不自然）
- 消音は localStorage の `pifpaf.sound`。既定は鳴らす。切り替えは共有の
  `Settings.tsx` にあるので、単機版とオンライン版の両方に同じものが出る
- 雑音は**1秒ぶんを一度だけ作って使い回す**。札の音は1ラウンドで100回近く鳴るので、
  その都度サンプルを埋めると配札の演出中に描画が引っかかる

### 鳴らす場所
**ボタンには配線しない。** 押した側だけ鳴らすと CPU や相手が引いて捨てる音が鳴らず、
卓に自分しかいないように聞こえる。それに単機版とオンライン版で同じ配線を2組持つ
ことになり、片方だけ増えてずれていく。

代わりに `game/useBoardSounds.ts` が、どちらの版も持っている数
（山札・捨て札の枚数、自分の番か、上がった席）の**差分**で鳴らす。
何が起きたかは数で分かるし、CPU の手にも音が付く。

札が動かない場面だけは例外で、そこは共有部品側で鳴らしている
（`TablePrompts` の JOGAR / CORRER / PASSAR / FICAR）。演出の音は演出の部品が持つ
（`DealingScene` `CardFlight` `CardBurst` `MoneyRain`）。

### 環境音（イントロの不穏な音）
「奥の部屋で待たされている数十秒」という設定。**曲ではないので旋律を置かない**
（聞き覚えができると、2回目から待ち時間の長さが気になり始める）。
低い持続音・弾かれる低音・増四度・盤の擦れの4層だけ。不穏さの出どころは3つ:

- ニ短調の主音を鳴らし続けたまま、**増四度（G#3）を薄く出し入れする**。
  23秒周期で、拍とも呼吸とも噛み合わない。協和に着地しないので終わった感じが出ない
- 低音の歩みが8拍目で**半音下（C#2）から主音に入る**。ジャズの常套だが、
  短調で遅く弾くと不吉になる
- 1周ごとに拍の長さをわずかに揺らす（等間隔だと機械が刻んでいるように聞こえる）

踏んだこと:
- **低音の減衰を1拍より長くすると歩みが潰れる。** 1.05秒（拍は0.91秒）にしていたら
  音が繋がって、歩いているのではなく持続音に聞こえていた。0.72秒に切って直った
- **増四度を高いところで鳴らすと単なる笛になる。** 1オクターブ下げてドローンの
  倍音（A2=110Hz）の隣に置くと、にごりが出て初めて不穏になる
- 上がりの刺しや金貨より十数dB下に置く。これは背景であって聞かせる音ではない
  （実測: 環境音の山 0.23 / 効果音の山 0.56）

流すのは**卓に着く前だけ**（イントロ・掛け金画面・オンラインの入口と待機画面）。
`useAmbience(active)` を鳴らす側の画面が持つ。

### 確かめ方
音は目で見えないので、**ヘッドレスChromiumで master の出力を測る**。
`context.ts` の `audio()` が返す `out` にアナライザを繋げば、画面ごとに
鳴っている／いないが数で出る（モジュールの同一性に依存しない見方なので、
Vite のHMRで実体が分かれていても正しく測れる）。
`MediaStreamDestination` + `MediaRecorder` で録れば、実際の音も取り出せる。

## ルールブック
`RuleBook.tsx` が組み立て、文面は `i18n` の `t.rules` が持つ。
**ルールを変えたら rules.md と全言語の辞書を直すこと**（片方だけ直すと、
遊ぶ人の見る決まりと実装がずれる）。

- 初回訪問時だけ自動で開く（localStorage の `pifpaf.rulesSeen`）
- イントロ・掛け金画面の「AS REGRAS」、盤面ヘッダーの「?」からいつでも開ける
- 全11節。実装の根拠は rules.md 側に置き、こちらは卓上の言葉に直したもの

## 配札の演出
ラウンド開始時に `DEALING` 画面を挟む（`DealingScene`）。
**engine は既に配り終えた結果を持っていて、それを再生しているだけ**。
ここで札を配っているわけではないので、ルールには一切関与しない。

流れ: 山を割る → 割れ目からヴィラを抜いてめくる（コリンガ確定）
→ 3枚ずつ3巡で配る → 山札が定位置へ収まる → 手札が順に並ぶ

- 位置は `[data-stock-pile]` `[data-vira-slot]` `[data-seat="N"]` を実測する
- コリンガはめくる瞬間まで `?` で伏せる（`viraRevealed`）
- 手札は配り終わるまで出さない。`.hand` の空箱で高さだけ確保して、
  並ぶ瞬間に盤面がずれないようにしてある
- **配札の速さは常に「じっくり」で固定**（`DEAL_SPEED_FACTOR = 1.5`、約4.8秒）。
  CPUの速さ設定とは切り離してある。一局に一度きりの見せ場なので、
  進行を速くしている人にも同じ間合いで見せたい（ユーザー指定）
- `prefers-reduced-motion` では出さない（進行は変わらず、すぐ本編に入る）

**踏んだこと: 依存配列の参照が毎描画で変わると演出が最初からやり直しになる。**
`aliveSeats` が毎回新しい配列だったせいでタイマーが再起動し、
コリンガが「山を割る」の途中で開いてしまっていた。
`useGame` 側で useMemo し、`DealingScene` 側も `dealtSeats.join(",")` を
依存にしている。

## 捨て札から札が飛ぶ演出
誰かが捨て札を拾ったとき、札が山から取った人の席へ飛ぶ（`CardFlight`）。
枚数の数字が変わるだけでは「誰が何を取ったか」が追えないため。

- 位置は `[data-discard-pile]` と `[data-seat="N"]` を querySelector で拾って
  実測する。自分の席は `.me` に `data-seat={HUMAN}` が付いている
- `left/top` は出発点に固定し、移動は `transform: translate()` で行う
- 拾いの検知は `useGame` 側。CPUの手を適用する直前に
  `DRAW from:"DISCARD"` と `INTERCEPT` を拾って `notePickup` する
  （適用後だと札が捨て札から消えていて何を取ったのか分からない）

## 上がり手の公開
ラウンド結果で勝者の手札を役ごとに分けて見せる（`MeldReveal`）。
分類は `classifyAsMelds` の戻り値をそのまま使い、**web側で役を判定しない**。
分類できなかった場合は素の手札を並べる（割り込みの形で念のため）。

## 捨て札への割り込み（手番外の上がり）
本家の「捨て札で上がる」ルール。詳細は `rules.md` 5.2節。
`AWAITING_INTERCEPT` という局面を挟み、`interceptQueue` に候補を優先順で持つ。

- **行動主体は `currentActor(state)` で取る。** 割り込み中は `currentPlayer` ではなく
  `interceptQueue[0]` が判断者。ここを間違えるとCPU駆動が止まる
- **拾った札は役に使われていなければならない。** これを課さないと、既に9枚
  揃っている者が何を捨てられても割り込めてしまい、実測でラウンドの83%が
  割り込み決着になった（条件を課しても83%だったので、これは正真正銘の完成形）
- 割り込みを入れると1ラウンドが 56→44 アクションに短くなる。
  相手3人 × 1ラウンド約22回の捨て札ぶん、上がる機会が増えるため

## CPUの速さ
`useGame.ts` の `SPEED_FACTOR`。はやい0.5倍 / ふつう1倍 / じっくり2倍で、
**既定は「ふつう」**（1手番＝引く＋捨てるでおよそ2秒）。localStorage に保存する。
選ぶ場所は設定画面（前節）。`useGame` は種別（`Speed`）と `setSpeed` だけを持ち、
**表示名は辞書の `t.speed`** にある。

この設定が効くのは**CPUの手番だけ**。配札の演出は常に「じっくり」固定で、
`App.tsx` の `DEAL_SPEED_FACTOR` が持つ。

## 終了演出の確認方法
終了演出は1局遊びきらないと見られず、勝敗も運任せなので、開発時だけ
URLで直接呼べるようにしてある（`useGame.ts` の `devScene`）。
```
http://localhost:5173/?scene=win    金が降る
http://localhost:5173/?scene=lose   破産（精算画面）
```
`import.meta.env.DEV` で囲ってあるので本番ビルドからは消える。

## 見た目の方針（ユーザー指定）
マフィアの酒場の奥の部屋、という設定。ノワール調（黒・生成り・金・血の赤、
明朝／Cinzel）。

**撃たれる演出は入れない**（2026-09-03・ユーザー指示。単機版・オンライン版とも）。
以前は破産した者を1人ずつ撃つ段取りがあったが、まるごと外した。
削除したもの: `useExecution`（撃つ順と間合い）、`BulletHole`（銃痕）、
`.muzzleFlash`（閃光）、`.seat--firing`、銃声（`sfx.shot`）、
イントロの「※演出です」の断り書き。戻したくなったら git 履歴にある
（さらに前のリボルバーの演出は `web/src/components/GunShot.tsx`）。

**イントロの環境音で鳴る一発は、これとは別もの**（2026-09-04・ユーザー指示）。
誰が撃たれたのかも見せず、盤面にも触らない。曲を断ち切るためだけの音で、
`ambience.ts` にある（`sfx` には置かない。盤面の出来事ではないため）。

**脱落したこと自体は今も見せる。** これは演出ではなく盤面の情報で、
誰がもう打たないのかが分からないと卓が読めない。
FALIDO／ELIMINADO のスタンプと、席と画面の白黒化だけが残っている。

判定は**チップが尽きたかどうか**そのもの（`chips <= 0`）。
段取りを外したので「撃たれ終わったか」を待つ必要がなくなり、
オンライン版の `SeatView.out` と同じ見方に揃った。

決着後の流れ:
1. 破産した者にスタンプが付き、席が白黒になる
2. 自分が勝っていれば、判定と一緒に**金貨と札束が降ってくる**（`MoneyRain`）

ラウンド単位の勝ちには別に軽い祝いがある。自分がラウンドを取ると、
**勝った手札そのものが放射状に飛び散る**（`CardBurst`、約1.4秒）。
マッチ制覇の金と役割を分けてあるので、どちらも短く保つこと。
祝いが終わるまで結果パネルは出さない（重ねると札が読めない）。
`prefers-reduced-motion` では出さず、待ちもしない
（CSSで隠すだけだと、何も見えないままパネルが1.4秒遅れる）。

あくまで記号的な表現にとどめ、生々しい描写はしない（血や傷は描かない）。
イントロに「※演出です」の注記を置いている。

## オンライン対戦（2026-09-02 に実装）
当初の計画どおり `server/` を足し、engine をそのまま Node 側に載せた。
**engine は1行も書き換えていない**（足したのは規則の穴が1つだけ。後述）。

### 入口はイントロだけ（2026-09-03 修正）
オンラインへ入る道は**イントロの ONLINE ボタンだけ**。URL からは入れない。

以前は `?online=1` を URL に持たせ、起動時にそれを読んでいた（再読み込みで
同じ場所に戻れるように）。書き込みが `history.replaceState` なのでアドレスに残り、
**一度オンラインを覗くと以後ずっとロビーが入口になる**。URL が保たれる置き方
（artifact など）では、開くたびに卓の設定画面から始まってしまっていた。

対価として、オンラインの対局中に再読み込みするとイントロに戻る。
席へはロビーから接続コードで入り直せる（再接続トークンは残っている）ので、
卓から締め出されはしない。

### 卓の作成と参加（2026-09-02 更新）
- ホストは `CREATE` を送り、サーバーが読み違えにくい4文字の接続コードを発行する
- 参加者は `JOIN` と接続コードで既存の卓に入る。存在しないコードから卓は作らない
- `RoomInfo.hostSeat` がホストを示し、ゲームを開始できるのはホストだけ
- 待機画面にはホスト・自分・参加者・空席を明示し、コードをコピーできる
- 通信仕様は `PROTOCOL_VERSION = 2`。旧画面は再読み込みを案内して弾く

### まず人を募り、集まらなければ CPU を呼ぶ（2026-09-03・ユーザー指示）
**待つのが既定。CPU で埋めるのは別の操作。**
`Room.start(fillWithBots = false)` の既定は4人そろうまで始まらない。
人が来ないときだけ、ホストが明示的に CPU を呼んで空席を埋められる。

開始そのものが空席を CPU で埋めていた頃は、人を待つ前に CPU 戦が始まってしまい、
オンラインに来た意味が薄れていた。かといって人間4人を必須にすると、
集まらない時間帯にまったく遊べない。**「待つ」を既定にし、「CPUで埋める」を
別の操作に分ける**ことで両方立つ。

- `START` に `fillWithBots?: boolean` を足した。壊れた値は「呼ばない」に倒す。
  **`PROTOCOL_VERSION` は上げていない**（省略時＝待つ、で古い画面も安全側に倒れる）
- 待機画面は COMEÇAR（4人そろうまで押せない）が主、
  CHAMAR A CPU が細い帯で下に付く。人が集まらないときだけ出る
- **切断席の CPU 代打ちはこれとは別物**（`isBotControlled` = BOT席 or 切断中）。
  対局中に人が抜けても卓が止まらないための仕組みで、卓の立て方とは無関係

### 打つ人が CPU だけになった対局は見せない（2026-09-04・ユーザー指示）
人が誰も打っていない対局を1手ずつ間合い付きで流しても、待たせるだけで
手を出せる場面はもう来ない。**決着まで飛ばして結果だけを配る**（単機版と同じ）。

- `Room.runsOnBotsAlone()` が判定、`Room.runOutRound()` が一気に打つ。
  間合いは今までどおり `hub.ts` の持ち物で、`room.ts` は時計を触らない
- 一気に打っているあいだは途中経過を配らない（`quiet`）。
  1手ずつ配ると盤面が高速で瞬くだけになる
- **見ている人が生きているかは問わない。** 降りた人も破産した人も、席に着いたまま
  CPU の応酬を見せられる立場は同じ。ここを「生きている人がいるか」で判定していた
  ときは、破産した人だけが1手ずつ見せられていた（実測で1マッチ 74秒 → 0秒）
- 誰も繋がっていない卓は飛ばさない。畳まれるのを待つだけで、急ぐ理由がない
- `maybeAdvance` は**破産した人も待つ**。繋がっている以上は結果を見ているので、
  勝手に次を配ると読む間もなく流れていく（CONTINUAR は全員の画面に出ている）
- `scheduleBot` は**保留中の間合いより先に**この判定を見る。前の局の予約が
  残っているうちに次の局が始まると、1手ぶん間合い付きで漏れる

### 動かし方
```
npm run server                  # ws://127.0.0.1:8787
npm run dev --workspace=web     # イントロの ONLINE から入る
```
繋ぎ先は `VITE_WS_URL` で差せる。**既定で localhost にしか出さない**
（外に出すときは `HOST=0.0.0.0` を明示）。

### 芯になっている決めごと
- **サーバーが権威。クライアントは意図だけ送る。** 盤面を送らせると改竄の
  検査が要るが、「その席の番か」だけ見れば済む形にしてある
- **配る型は `GameState` から派生させない。** `PublicGameState` は別物として
  書いてある。共通の親から派生させると、engine に伏せ札を足したときに黙って漏れる
- **`Room` は通信手段もタイマーも知らない。** 変化したら `onChange` を呼ぶだけ。
  CPU も自分では動かず、`needsBotStep()` / `stepBot()` を外から回す。
  Cloudflare Durable Objects へ移すとき書き換えるのが transport だけで済み、
  テストに時計を差し込む必要もない（卓の検査19件はすべて素の同期コード）
- **決めるのはサーバー。** web が送った手はサーバーが必ず検め直す。
  ただし**「上がれるか」だけは web でも engine の `findBaterAction` を呼ぶ**
  （2026-09-04 修正）。理由は2つで、どちらも譲れない:
  - 捨てる札を指さない `BATER` は「10枚すべてが役」の意味しか持たない。
    いちばん普通の「1枚捨てて9枚で上がる」を送るには、どの札を捨てるかを
    web が決めるしかない。**ここを省いていたため、オンラインでは9枚上がりが
    1度も通らなかった**（模擬で89件中89件が拒否。通ったのは10枚上がりだけ）
  - 上がれるかが見た目に出ないと、ボタンが手札と無関係に点く。実際、
    自分の捨てる番なら必ず押せて、注記も常に「上がれる」と出ていた
  engine は単機版のために web へ元から入っているので、増えるものは無い。
  権威はサーバーのままで、web がやるのは合図と手の組み立てだけ

### 結果画面は全員が押すまで進まない（表示を足した・2026-09-04）
次のラウンドは繋がっている人が全員 CONTINUAR を押すまで始まらない。
**押しても画面が何も変わらないと、届いていないのか卓が止まったのか分からない。**

- `RoomSeat.ready`（その席が押したか）を配る。**`PROTOCOL_VERSION` は上げない**。
  足しただけなので、古い画面は読み飛ばすし、新しい画面が古いサーバーに繋がっても
  「押しても変わらない」以前の見え方に落ちるだけで壊れない
- 押した側はボタンを閉じ、注記を「ほかの人を待っている…」に変え、
  **誰を待っているかを名前で**出す。CPU と切れている席は待たないので出さない
  （サーバーの `maybeAdvance` と同じ見方）
- 名前の連ね方は言語で違う（「、」と ", "）ので、辞書には配列を渡す

### 席が抜けたときの扱い
**席は空けない。操作だけ CPU に渡す。** 対局中に席を消すと残りが続けられない。
戻ってくればトークンで同じ席に戻れる。

**手番だけ CPU に渡しても足りない。** 「降りるか決める」「結果画面で次へ」の
2か所でも人を待っているので、切断時にその判断も肩代わりしないと卓が永久に止まる
（実際に止まり、テストが掴んだ）。

### engine に足した規則
`walkoverWinner` … 降りた結果、打つ人が1人以下になったラウンドの扱い。
1人だけ残ったら不戦勝。これが無いと残った1人が延々と引いて捨てるだけになる。
単機版にも同じ場面の処理はあったが、打っていないのに手札のワイルドを
「使った」と数えて配当が0.75倍になる欠陥があったので、engine の判定に寄せた。

### 単機版と揃えてある部分（2026-09-03）
**通信の都合以外は単機版と同じ器を使う。** オンライン版に写しを置いた結果、
同じ場面なのに見え方が違う箇所ができていたので、共有部品に寄せた。

| 部品 | 場所 | 揃う前の状態 |
|---|---|---|
| 相手の席 | `components/OpponentSeat.tsx` | オンラインは帽子・肩書き・思考中・ELIMINADO が無かった |
| 上がり手の公開 | `components/MeldReveal.tsx` | 同じものが2か所に写してあった |
| 降りる判断・採否・割り込み | `components/TablePrompts.tsx` | オンラインは失点の注釈と手持ちチップが落ちていた |
| 設定 | `components/Settings.tsx` | オンラインには歯車が無かった |

`OpponentSeat` は persona を受け取らず、名前と肩書きを**呼び出し側が組み立てて渡す**。
persona を持ち込むとオンライン版だけ別の見た目に育つ（単機版は i18n の personas から、
オンライン版は `RoomSeat` と `SeatView` から作る）。

**卓を抜ける動線。** ヘッダーに × は置かない。単機版に対局中の離脱が無いので、
オンラインだけ抜けられるのは筋が通らない（ユーザー指示・2026-09-03）。
出口は2か所だけ:
- 待機画面（`WaitingPanel`）の `SAIR` … まだ始まっていないので抜けられる
- 決着後（`ResultPanel` の `MATCH_OVER`）の `VOLTAR À MESA`
どちらも `disconnect()` で卓の入口（`Lobby`）に戻る。**決着後の出口を消すと詰む**
（`MATCH_OVER` では `CONTINUAR` が出ないため、他に進む道が無い）。

**ラウンド番号は結果表示中だけ1つ戻す。** `settleRound` が `round + 1` を返すので、
サーバーの `RoomInfo.round` は結果を見せている最中すでに次を指している。
単機版が同じ扱いをしているので、揃えないとオンラインだけ1つ先に見える。

### まだ載せていないもの
配札の演出・札が飛ぶ演出・勝ちの祝い。どれも単機版では `GameState`
（全員の手札）を前提に組んであるため、そのままでは持ってこられない。
**先にやるべき整理**: UI が扱う盤面の型を `PlayerView` に揃える。単機版も
自分の状態を `maskFor` に通せば同じ型になるので、盤面の画面を1つにできる。
共有部品に寄せたぶん差は縮んだが、`App.tsx` と `OnlineTable.tsx` という
2つの盤面はまだ残っている。

### 公開構成（Render + GitHub Pages）
GitHub Pages が画面、Render Web Service が卓。**画面と卓は別の場所に居る**
（Pages は静的配信しかできず WebSocket を置けない）。

**2026-09-04、Render のサービスは作成済み**（`pifpaf-online-kongroo`、名前は既定のまま）。
以後この手順を踏み直す必要はない。

繋ぎ先は `useOnlineGame.ts` の `RENDER_WS_URL` に焼いてある。
`render.yaml` の `name` から URL が決まるので、GitHub の変数設定は要らない。
別名で立て直したときや他所へ移したときだけ、Repository variable `VITE_WS_URL` で
差し替える（`deploy.yml` が渡していて、空なら焼いた既定に落ちる）。

**繋がらないときに最初に疑うのはこの変数**。焼いた既定より優先されるので、
昔の値が残っていると、そちらへ繋ぎに行って失敗し続ける。

**この開発環境から Render と GitHub Pages へは出られない**（egress ポリシーで
CONNECT が 403）。実地の疎通確認はブラウザから行うしかない。
`https://pifpaf-online-kongroo.onrender.com/health` が
`{"ok":true,"rooms":[...]}` を返せば卓は生きている。

無料枠は15分間通信がないと休止する。休止後の最初の接続は失敗しうるが、
`useOnlineGame` が待ち時間を倍にしながら繋ぎ直すので、
1分ほどで自然につながる（画面は「卓との通信を戻しています…」のまま待つ）。

### import に拡張子を明示している
Node の ESM は拡張子を省略できないため、engine / protocol / server の
相対 import には `.ts` を付けてある（`allowImportingTsExtensions` を
`tsconfig.base.json` に置いた）。ビルド段階や tsx のような依存を足さずに
`node --experimental-strip-types` で直接動かすため。**新しいファイルを
足すときも拡張子を付けること。** 忘れるとサーバーだけ起動しなくなる。

### CI
公開の関門（`deploy.yml`）には**決定性のあるものだけ**を置く（engine と
protocol）。実際にポートを開く server の e2e は `ci.yml` に分けてある。
落ちたときに web の公開まで止まるのは割に合わないため。

## 現在の実装状況
- `engine/types.ts`：完成。Card/Suit/Rank。
  **ランクの並びが2種類ある。取り違えないこと。**
  - `SEQUENCE_AXES`（A が上／A が下の2本。どちらも折り返さない）… 階段の判定と
    AIの隣接判定（`sequenceDistance`）。**A は両端で使えるが、またげない**
  - `SEQUENCE_ORDER`（`SEQUENCE_AXES[0]`）… 手札の整列に使う見た目用の並び
  - `RANK_ORDER`（A始まりの循環）… ヴィラの「次のランク」を出す `nextRank` 専用
- `engine/deck.ts`：完成。ジョーカーなし52枚×2組=104枚、Fisher-Yatesシャッフル
  （rng注入可＝テスト・再現性のため）、4人へ9枚ずつ配札、ヴィラ公開とワイルドランク決定
- `engine/melds.ts`：完成。
  - `isValidTrinca`：同ランク3〜5枚、ワイルド代用可。記号の重複数＝枚数−3
  - `isValidSequence`：同スート連番3枚以上、ワイルド代用可。
    **A は上にも下にも使えるが、またげない**（Q-K-A ○ / A-2-3 ○ / K-A-2 ✕）。
    軸1本の循環で判定するとまたぎが通り、A を上に固定すると A-2-3 が落ちる
  - `classifyAsMelds`：手札全体を役に分類できるか全探索で判定（9〜10枚程度なら実用速度）
- `engine/gameEngine.ts`：完成。reducerスタイルの `applyAction(state, action)`。
  不正な操作は例外を投げず `{ ok: false, error }` で返す。
  `DRAW` は `from: "STOCK" | "DISCARD"` を取る（省略時は STOCK）。
  **一番手の最初の手番だけ専用の局面がある**（`rules.md` 5.1節）:
  `AWAITING_FIRST_DRAW`（ヴィラを買う `TAKE_VIRA` か、山札から引く）→
  引いた場合は `AWAITING_KEEP_DECISION`（`KEEP` で手札へ / `REJECT` で捨てて引き直し）。
  引き直しは1回だけで、そのあとは通常の `AWAITING_DRAW` に戻る。
  - **ヴィラは `GameState.vira` が持つ。** 買われると null になる。
    以前は web 側のReact stateに置いていたが、買えるようになった時点で
    ゲーム状態そのものなので engine に移した。
  - カード保存の不変条件は `手札 + 山札 + 捨て札 + 場のヴィラ + 見せている札 = 104`。
    「ヴィラは常に場外だから103」ではなくなっている（統合テストがこれを検査する）。
- `engine/ai.ts`：完成。`cardAffinity` で「役への絡み具合」を点数化し、最も孤立した札を
  捨てる。`shouldTakeDiscard` で捨て札を拾うかを判断する（拾えば上がれる／ワイルド／
  役に確実に絡む、のいずれかのときだけ拾う消極的な判断）。
  `findBaterAction` は人間側の「BATER」ボタンの活性判定にも使っている
  （ルール判定をweb側に持たせないため）。
- **テストは100件すべて通過**（`npm test --workspace=engine`）。
  CPU4人による通しプレイの統合テストを10シード分含む（決着すること・カードが増減しないこと）。
- `engine` / `web` とも `tsc --noEmit` がクリーン。
  以前は `noUncheckedIndexedAccess` 由来のエラーが deck/melds/types に残っていたが解消済み。
- `web/`：マッチ制（7チップ・複数ラウンド・掛け金と配当）として動作確認済み。

### 300局まわしたときの様子（AI同士）
| | 山札のみ | ＋捨て札ドロー | ＋ワイルドを同スートのみに |
|---|---|---|---|
| 1局の長さ（中央値） | 34 | 28 | **56** アクション |
| 最長 | 100 | 76 | 144 |
| 引き分け | 0 | 0 | 0 |

ワイルドが8枚→2枚に減ったぶん、**長さが約2倍**になった。
勝者は席ごとにほぼ均等（75/86/71/68）。ドロー元は捨て札46.4%。
山札の組み直しは300局で0回（＝安全弁は通常のプレイでは働かない）。

CPUの思考待ちは1手あたり0.9〜1.1秒なので、1局の待ち時間もほぼ倍になっている。
間延びして感じるなら `useGame.ts` の `CPU_DRAW_DELAY` / `CPU_DISCARD_DELAY` を縮める。

### 手札の並べ替え（web側だけの機能）
engineはカードをIDでしか見ないので、並び順を変えてもルールには影響しない。
`useHandOrder` が表示順を持ち、`PlayerHand` が Pointer Events でドラッグを処理する。
- **HTML5 の drag-and-drop は iOS Safari で動かない。** Pointer Events で自前で組むこと
- `touch-action: none` が無いと、スマホでドラッグがページのスクロールに取られる
- タップ（選択）とドラッグ（並べ替え）は移動量8pxで区別している
- **カードIDは "S-7-d1" のようにデッキ内で固定** なので、配り直しても前局と重なる。
  「IDが残っている＝手札に残った札」という差分判定は配り直しで誤作動するため、
  局の切り替わりは `gameId` で明示的に受け取っている（実際に踏んだ）

## rules.md に明記した「実装上の仮定」（要ユーザー確認・後で切り替え可能に）
- ラウンド中の途中メルド公開は無し（一発上がり形式）
- ヴィラのカードは山札にも捨て札にも混ぜない
- 全部ワイルドのトリンカは許容、全部ワイルドのシーケンスは不許可
- 捨て札から拾った札は、その手番では捨て直せない（上がりで余らせるのは可）
これらはゲームロジック内に決め打ちで実装されているため、後で設定可能にする場合は
`engine/melds.ts` と `engine/deck.ts` を要修正。

## 出典で確認したルール（2026-08-31 に調査）
`rules.md` の 9〜11章に出典URLつきでまとめてある。当初「実装上の仮定」として
保留していたが、調べて確定したもの:
- **ドローは山札か捨て札のどちらかを選べる。** 捨て札から取れるのは一番上の1枚だけ
  （表向きなのがその1枚だけのため）。当初は「捨て札からのドロー＝不可」としていた
- **山札が尽きたら捨て札がそのままの順で山札になる**（シャッフルしない）。
  順序を保つので乱数が要らず、「シャッフルには乱数が要るから引き分けにする」という
  当初の判断は不要だった。`applyAction` の純粋性を保ったまま実装できている
- 上がりは10枚中9枚以上 — 既存の実装と一致していた

## 動かし方
```
npm install                        # ルートで1回
npm test                           # 全ワークスペース（184件）
npm run typecheck                  # 全ワークスペース
npm run dev --workspace=web        # http://localhost:5173
npm run server                     # ws://127.0.0.1:8787（オンライン対戦）
```

## 次のタスク（優先順）
1. **盤面の型を `PlayerView` に揃える**：いま単機版（`GameState`）と
   オンライン版（`PublicGameState`）で盤面の画面が2つある。放っておくとずれる。
   単機版も `maskFor(HUMAN, state, match)` を通せば同じ型になるので、
   1つに畳める。**配札の演出・札が飛ぶ演出・勝ちの祝いをオンラインへ
   持っていくには、先にこれが要る。**
2. **公開先の決定**（ユーザーの承認が要る）：GitHub Pages に WebSocket は
   置けない。→ オンライン対戦の節を参照
3. **AIの強化**：今は自分の手札しか見ていない。捨て札の履歴を見て危険牌を避ける、
   ワイルドの温存判断を入れる、など。`cardAffinity` の重み調整でかなり変わるはず。
4. **人間のBATER時の捨て札選択**：`findBaterAction` は「上がれる組み合わせ」を
   1つ見つけて返すだけなので、余り札が複数あり得る場合に選ばせていない。
5. オンラインの賭け金・配当：いまはチップだけで、掛け金は載せていない。
   金銭の見せ方は仕様の判断が要る。

## コーディング規約（このプロジェクト内で守ってきたこと）
- `engine/` 配下は副作用ゼロの純粋関数のみ。DOM・React・fetch・WebSocketへの依存禁止
- `protocol/` も同じ（型と純粋関数のみ）。通信手段を知らせない
- `server/room.ts` に `setTimeout` を書かない。間合いは `hub.ts` の担当
- **相対 import には `.ts` を明示する**（engine / protocol / server）。
  Node が直接読むため。付け忘れるとサーバーだけ起動しない
- 乱数は関数引数として注入可能にする（テスト再現性のため）
- コード内コメントは日本語で統一
- 型はできるだけ厳密に（`tsconfig.base.json` は `strict: true` + `noUncheckedIndexedAccess: true`）
